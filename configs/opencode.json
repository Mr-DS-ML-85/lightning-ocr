{
  "_comment": "OpenCode MCP config — ~/.config/opencode/config.json OR opencode.json in project root.",
  "mcp": {
    "lightning-ocr": {
      "type": "local",
      "command": ["uv", "run", "--directory", "/path/to/lightning-ocr", "python", "-m", "connector.transport_stdio", "--legacy25"],
      "enabled": true
    }
  }
}
